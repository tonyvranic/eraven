---
import Image from '../Image.astro';
import MediaBorder from '../media-border/MediaBorder.astro';

/* Props */
interface Props {
  isSecondary?: boolean;
  isVertical?: boolean;

  imageSrc: string;
  imageAlt: string;
  imageWidth: number;
  imageHeight: number;

  imageDataOffset?: string;
  imageDataMultiplierOffset?: string;
  imageDataMultiplier?: string;

  classes?: string[];
}

const { 
  isSecondary,
  isVertical,

  imageSrc,
  imageAlt,
  imageWidth,
  imageHeight,

  imageDataOffset,
  imageDataMultiplierOffset,
  imageDataMultiplier,

  classes: classes = [],
  ...rest
} = Astro.props;


/* Computed */
const columnMediaThemeClasses = !isSecondary ? ['primary-700'] : ['secondary-700', 'hover-secondary-400'];
const overlayThemeClasses = !isSecondary ? ['bg-primary-600'] : ['bg-secondary-600'];

const isTypeVerticalClasses = isVertical ? ['type-vertical'] : [];
---


{/* Html */}
<div class:list={['column-media', 'rel', ...isTypeVerticalClasses, ...columnMediaThemeClasses, ...classes]} {...rest}>
  <MediaBorder />
  <div class:list={['column-media-container', 'media', 'rel', 'z-1']}>
    <Image
      src={imageSrc}
      alt={imageAlt}
      width={imageWidth} height={imageHeight}

      data-offset={imageDataOffset}
      data-multiplier-offset={imageDataMultiplierOffset}
      data-multiplier={imageDataMultiplier}
    />
    <div class:list={['column-media-overlay', 'abs-fw', 'z-2', 'no-e', ...overlayThemeClasses]}></div>
  </div>
</div>


{/* Styles */}
<style lang="scss">
  @use './ColumnMedia.scss';
</style>


{/* Script */}
<script>
  
</script>